import { styled, Typography } from "@mui/material";

export const InfoTitle = styled(Typography)(({ theme }) => (
    {
        color: '#21a5f1'
    }
))

export const InfoValue = styled(Typography)(({ theme }) => (
    {
        color: 'yellow'
    }
))

export const InfoUnit = styled(Typography)(({ theme }) => (
    {
        color: 'white'
    }
))